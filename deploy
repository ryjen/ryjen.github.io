#!/usr/bin/env bash

if [ "$1" == "force" ]; then

  git push dokku `git subtree split --prefix server master`:master --force

  git push github `git subtree split --prefix server/public master`:master --force

else

  git subtree push --prefix server dokku master

  git subtree push --prefix server/public github master

fi
